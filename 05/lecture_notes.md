# bruin

- bruin mcp to install on vscode
- bruin init
- .bruin.yml should not be pushed as it will contains credentials
- pipelines.yml (orchestration, name of the pipeline, connector, start_date...)